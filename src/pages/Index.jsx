import React from 'react';

const Index = () => {
  return (
    <div>
      {/* This page is no longer used as we moved everything to App.jsx */}
      {/* Keeping it for routing compatibility */}
    </div>
  );
};

export default Index;
